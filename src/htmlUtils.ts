export const getStrong = (text: string): string => `<strong>${text}</strong>`;
export const getItalic = (text: string): string => `<i>${text}</i>`;
export const getUnderline = (text: string): string => `<u>${text}</u>`;
export const getLine = (): string => `<br/>`;
